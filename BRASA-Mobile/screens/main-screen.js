// the main screen will also be the "news" screen

import { Text, View } from 'react-native';

const MainScreen = () => {
    return (
        <View>
            <Text>Olá</Text>
        </View>
    );
}

export default MainScreen;