import { Text, View } from 'react-native';

const NoVoting = () => {
    return (
        <View>
            <Text>There is no elections being held right now</Text>
        </View>
    );
}

export default NoVoting;